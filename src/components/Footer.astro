---
import { footerMenu } from '../data/menu.ts';
---

<footer class="bg-background-dark text-white pt-base pb-large">
  <nav>
    <ul>
      {footerMenu.map(item => (
        <li><a href={item.link}>{item.name}</a></li>
      ))}
    </ul>
  </nav>
</footer>